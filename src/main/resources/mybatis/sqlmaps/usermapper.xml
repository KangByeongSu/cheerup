<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="UserMapper">

	<select id="checkUser" parameterType="com.skplanet.project2.model.UserDTO" resultType="com.skplanet.project2.model.UserDTO">
		    SELECT 
				count(*) as count,
				IFNULL(u_nickname, '') as uNickname
		    FROM
		      Elephant_User user
		    WHERE
				u_id=#{uId} and u_pw=#{uPw}
	</select>



	<update id="updateUser" parameterType="com.skplanet.project2.model.UserDTO">
	
		update 
		Elephant_User 
		
		set u_nickname=#{uNickname},u_pw=#{uPw}
		
		where
		u_id=#{uId}
	
	</update>
	
	
</mapper>